{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Hello {{ controller_name }}! ✅</h1>
        <ul>
            <li>Connect with <code><a href="{{ path('connect_twitch_start') }}">twitch</a></code></li>
            <li>Connect with <code><a href="{{ path('connect_google_start') }}">google</a></code></li>
        </ul>
        <ul>
            <li><a href="{{ path('updateStreamInfos') }}">Change title</a></li>
        </ul>
    </div>
{% endblock %}
